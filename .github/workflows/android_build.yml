name: Android Build

on: pull_request

jobs:
  build:

    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v2

      - name: Run Tests
        run: .gradlew check

      - name: Build
        run: .gradlew build
